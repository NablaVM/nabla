.file "modload"

.init main

;   Devices are 'hardware' modules that extend the functionality of the vm
;   As-apposed to a HLL 'import' that just brings in code, the devices, will 
;   instantiate 

; Maybe we don't instantiate when called. Maybe we always connect them like actual hardware.... but these 'setup' the hardware
.mod.io.module_name
.mod.net.module_name

<main:



>
