

def main() -> int {

    int start     = 1;
    int end       = 10;
    int step_amt  = 2;


    for x in range:int(start, end) step step_amt
    {
        int inner_range = x /2;

        int inner_step = 1;

        for y in range:int(x, inner_range) step inner_step
        {
            if(x % 2 == 0)
            {
                x = x + 1;
            }
        }
    }

    return 0;
}