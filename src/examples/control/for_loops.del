

def main() -> int {


    for x in range:int(1, 10) 
    {
        int a = x;
        int b = a;
        int c = 1;
        int d = 4;
        real z = 0.01;

        for j in range:int(1, 10)
        {
            int p = 0;
            z = z + 0.01;

            p = a + 1;
        }

    }

    return 0;
}