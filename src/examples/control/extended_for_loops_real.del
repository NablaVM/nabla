

def main() -> int {

    real start     = 1.0;
    real end       = 100000.0;
    real step_amt  = 2.0;


    for x in range:real(start, end) step step_amt
    {
        real inner_range = x /2.0;

        real inner_step = 1.0;

        for y in range:real(x, inner_range) step inner_step
        {


        }
    }

    return 0;
}