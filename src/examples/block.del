

def main() -> int {

    // int b =  1 + 1;
    // b = 1 - b;
    // b = 10 / 5;
    // b = b * 2;
    // b = b << 10;
    // b = b >> 10;
    // b = b or 1;
    // b = not b;
    // b = 3 and 4;
    // b = 1 xor 0;

    //int a = 10 || 20;

    //int a = 10 && 20;

    //int a = !0;

    // int c =  2 ^ 8;

    // int  a = 1;
    real b = 3.1;

    b = 3.1 + 0.9;

    // char c = "j";

    return 0;
}