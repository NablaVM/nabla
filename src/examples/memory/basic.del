

def main() -> int {

    // Create a pointer
    int * p = new int [1];

    // The value held in ls here should be the valie assigned to a function call that holds the memory unit


    return 0;
}